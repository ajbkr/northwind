DATETIME -> INTEGER, REAL, or TEXT
DECIMAL -> REAL (or INTEGER?)
LONGTEXT, VARCHAR -> TEXT

DEFAULT: column_default

JSON Schema validation

Data type translation
	MySQL -> Sequelize
	MySQL -> SQLite
	MySQL -> Django ORM
