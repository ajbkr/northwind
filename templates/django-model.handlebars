from django.db import models


class {{pascalcase table_name}}(models.Model):{{#each columns}}
    {{this.column_name}} = models.{{dataType 'django' this.data_type}}Field(
    {{~#compare this.data_type '==' 'VARCHAR'~}}
        max_length={{#if this.character_max_length}}{{this.character_max_length}}{{else}}255{{/if}},
    {{~/compare~}}
    {{~#compare this.column_key '==' 'PRI'~}}
        primary_key=True,
    {{~/compare~}}
    )
{{~/each}}

